# Task 6：技术可行性分析

## 1. 本任务结论

SenseLoop 的技术路线必须分成三层：

1. **24 小时比赛可跑通层**：用模拟数据、上传文件、规则引擎、静态知识库和前端展示跑通完整闭环。
2. **现场接入增强层**：预留 soundcore Work SDK、eufy SDK、API Key、真实音频/图片上传接口，现场拿到能力后替换适配器。
3. **未来产品化层**：端侧音频识别、端侧图像识别、穿戴传感器、长期趋势、隐私计算和硬件集成。

当前最优技术判断：

> 不要在比赛第一阶段追求真实识别所有信号，而要先跑通“数据进入 -> 标签化 -> 知识库匹配 -> 每日建议 -> 页面展示”的闭环。真实 SDK、真实音频模型、真实图像模型都应该以适配器形式预留。

## 2. 24 小时可跑通目标

必须跑通：

- 用户画像选择。
- 夜间声音事件时间线。
- 打鼾、咳嗽、起夜、环境噪声、重点标记标签。
- 饮食、排便、舌苔、睡眠感受等补充输入。
- 每日健康报告生成。
- 五类用户切换后建议不同。
- 当前原型与未来手表/手环演进展示。

可以模拟：

- 真实 soundcore Work SDK。
- 真实 eufy 视频流。
- 真实音频识别结果。
- 真实舌苔识别结果。
- 真实饮食 kcal 识别结果。
- 心率、血氧、脉搏、运动数据。

不可乱承诺：

- 医疗级睡眠呼吸暂停识别。
- 疾病诊断。
- 自动拍摄隐私内容。
- 精准 kcal。
- 精准舌诊。
- 口气硬件检测。

## 3. 夜间声音识别可行性

### 3.1 目标能力

夜间声音模块需要识别或展示：

- 打鼾。
- 咳嗽。
- 呼吸节律片段。
- 翻身/床铺摩擦。
- 起夜。
- 环境噪声。
- 用户重点标记。

### 3.2 技术难度

| 能力 | 难度 | 当前建议 |
| --- | --- | --- |
| 音频上传/导入 | 低 | 直接实现 |
| 音频时间线展示 | 低 | 直接实现 |
| 打鼾检测 | 中 | 可先用模拟标签，后接模型 |
| 咳嗽检测 | 中 | 可先用模拟标签，后接模型 |
| 起夜/翻身识别 | 中高 | 用事件标签模拟 |
| 呼吸节律分析 | 高 | 当前只做趋势提示，不做真实判断 |
| 医疗级呼吸异常判断 | 很高 | 不做 |

### 3.3 推荐实现路线

比赛版本：

- 前端上传音频或选择内置样本。
- 后端/前端读取一组预置 `sleepAudioEvents`。
- 页面展示夜间声音时间线。
- 每个事件包含：类型、开始时间、持续时间、强度、置信度、是否来自真实识别。
- 当前默认 `source = mock | manual | sdk_placeholder`。

可选增强：

- 如果现场能拿到音频文件，先不做复杂模型，允许用户手动标注或半自动标注。
- 如果有时间，可接入 YAMNet / MediaPipe Audio Classifier 做通用声音事件候选识别。

未来路线：

- soundcore Work SDK 音频接入。
- 端侧音频事件模型。
- 长期趋势分析。
- 本地只保存事件标签，不保存完整原始音频。

### 3.4 可选技术选型

Web 原型采集：

- 浏览器使用 `navigator.mediaDevices.getUserMedia()` 请求麦克风权限。
- 使用 `MediaRecorder` 录制音频片段。
- 本地上传或保存为 Blob。

音频分类候选：

- YAMNet：可预测 AudioSet 中的 521 类声音事件，适合做通用声音分类候选。
- MediaPipe Audio Classifier：可使用 TFLite 音频分类模型，并支持音频流模式。

注意：

- YAMNet/MediaPipe 是候选方案，不是比赛必须实现。
- 真实睡眠声音识别仍需要定制数据和阈值校准。

## 4. 打鼾检测、咳嗽检测、呼吸节律分析

### 4.1 打鼾检测

可行性：

- 打鼾通常有明显低频、周期性、持续片段特征。
- 公开通用模型可能可以粗略识别，但家庭环境误报较多。

当前策略：

- Demo 中用标签化事件展示。
- 事件字段保留 `confidence`。
- 建议文案只说“打鼾片段较多/较少”，不做疾病推断。

### 4.2 咳嗽检测

可行性：

- 咳嗽声音事件短促、突发，识别比呼吸节律更容易。
- 但夜间可能与清嗓、说话、环境声混淆。

当前策略：

- Demo 用 `cough` 事件标签。
- 连续多天增加时提示“建议继续观察”。
- 不判断感冒、支气管、肺部疾病。

### 4.3 呼吸节律分析

可行性：

- 真正的呼吸节律分析需要较清晰音频、降噪、片段分离和连续信号处理。
- 对多人睡眠、空调声、被子摩擦、手机距离非常敏感。

当前策略：

- 不做实时呼吸节律算法。
- 用“呼吸相关声音片段”或“夜间声音连续性”表达。
- 如需展示，用模拟趋势图，不做诊断。

### 4.4 推荐技术定位

当前定位：

> 我们不是做医疗级睡眠监测，而是把 soundcore Work 捕捉到的夜间声音事件转化为用户能理解的恢复建议。

## 5. 大便识别可行性

### 5.1 技术难点

- 隐私敏感度极高。
- 自动拍摄存在伦理风险。
- 光线、角度、遮挡、水面反光都会影响识别。
- 颜色判断受环境光影响。
- 分类标准需要统一，例如 Bristol stool scale。
- 用户接受度不确定。

### 5.2 当前策略

当前原型只做：

- 手动记录。
- 标签选择。
- 可选用户主动上传图片。
- 只保存结构化标签。

标签示例：

- `hard`
- `normal`
- `loose`
- `watery`
- `dry`
- `dark`
- `red_flag`

### 5.3 不做什么

- 不做自动拍摄。
- 不用 eufy 拍厕所。
- 不保存默认原图。
- 不做疾病判断。

### 5.4 未来路线

- 用户主动触发拍照。
- 端侧识别。
- 只保存类型、颜色、频率、异常标记。
- 异常连续出现时建议咨询医生。

## 6. 舌苔识别可行性

### 6.1 技术难点

- 光照影响颜色。
- 手机摄像头差异明显。
- 舌头伸出角度、唾液反光、模糊都会影响判断。
- 中医知识库需要谨慎表达。
- 不能输出疾病诊断。

### 6.2 当前策略

比赛版本：

- 手机拍照上传或使用示例图。
- 用户可手动选择标签。
- 系统输出生活方式建议。

标签示例：

- `pale_tongue`
- `red_tongue`
- `thick_coating`
- `thin_coating`
- `dry`
- `teeth_marks`
- `cracks`

建议表达：

- “建议结合睡眠、饮食、补水继续观察。”
- “今天减少油腻，注意补水。”

### 6.3 未来路线

- 拍摄质量检测。
- 光照提示。
- 舌体分割。
- 颜色校正。
- 端侧标签化。

## 7. 口气检测可行性

### 7.1 技术难点

- 需要额外气味/VOC 传感器。
- 穿戴设备体积、功耗、精度、成本都需要验证。
- 口气与饮食、口腔卫生、肠胃、睡眠都有关，不能简单归因。

### 7.2 当前策略

当前原型只做：

- 问答反馈。
- 用户手动选择。
- 模拟口气等级。

标签示例：

- `none`
- `mild`
- `obvious`
- `morning_dry_mouth`
- `bitter_taste`

### 7.3 未来路线

- 气味/VOC 传感器。
- 与舌苔、饮食、睡眠、排便联动。
- 社交提醒和生活方式建议。

## 8. 饮食 kcal 识别可行性

### 8.1 技术难点

- 食物种类识别相对容易。
- 份量估算很难。
- 单张图片缺少深度信息。
- 中餐混合菜、盖饭、汤类、外卖包装都会影响识别。

### 8.2 当前策略

采用：

> AI 估算 + 用户修正

当前原型：

- 上传或模拟食物图片。
- 输出食物类别。
- 输出 kcal 区间，而不是精确值。
- 用户修正份量。

字段示例：

- `foodName`
- `portion`
- `estimatedKcal`
- `confidence`
- `userAdjustedKcal`

### 8.3 建议表达

可以说：

- “这餐热量可能偏高。”
- “建议晚餐减少油脂和精制碳水。”
- “今天总摄入建议控制在目标范围内。”

不要说：

- “精确计算热量。”
- “拍一下瞬间知道准确 kcal。”

## 9. 哪些数据端侧处理，哪些数据可云端

### 9.1 必须优先端侧或本地处理

- 原始夜间音频。
- 舌苔照片。
- 排便照片。
- 口气相关数据。
- 家庭摄像头画面。

处理原则：

- 默认不上传原始敏感数据。
- 优先提取标签。
- 上传标签和摘要，而不是上传原始内容。

### 9.2 可上传或云端生成

可上传内容：

- 用户主动授权后的标签数据。
- 用户画像。
- 结构化夜间声音事件。
- 饮食标签。
- 排便标签。
- 舌苔标签。
- 今日建议生成所需摘要。

云端适合做：

- 个性化报告生成。
- 多日趋势总结。
- 知识库检索。
- 文案生成。

### 9.3 本地原型建议

比赛原型建议全部本地化：

- 前端内置模拟数据。
- 本地 JSON 知识库。
- 本地规则引擎。
- 可选 API Key 接入 LLM。
- 无 API Key 时使用预置报告模板。

## 10. 推荐技术路线

### 10.1 当前比赛版

架构：

```text
模拟/上传数据
  -> 事件标签化
  -> 用户画像匹配
  -> 本地健康知识库
  -> 规则引擎生成建议
  -> 前端日报展示
```

必须实现：

- `mockData`。
- `userProfile`。
- `sleepAudioEvents`。
- `dailySignals`。
- `knowledgeRules`。
- `recommendationEngine`。
- `DailyReport` 页面。

可选实现：

- 上传音频文件。
- 上传图片文件。
- API Key 接入 LLM。
- SDK adapter 占位。

### 10.2 现场增强版

如果拿到 SDK：

- `soundcoreAdapter` 替换 mock audio events。
- `eufyAdapter` 替换 mock visual events。
- 保持上层数据结构不变。

如果拿到 API Key：

- 用 LLM 生成更自然报告文案。
- 仍要求结构化 JSON 输出。
- 无 API Key 时回退规则模板。

### 10.3 未来产品版

- 端侧声音分类。
- 端侧图像标签。
- 穿戴传感器。
- 长期趋势。
- 多设备家庭健康视图。
- 隐私计算和本地优先架构。

## 11. 推荐项目技术栈

### 11.1 前端

推荐：

- React + TypeScript + Vite。
- 本地 JSON mock 数据。
- 单页 App。
- 手机 App 风格界面。

原因：

- 开发快。
- 适合 24 小时比赛。
- 易做交互和页面切换。
- 可直接部署为静态页面。

### 11.2 业务层

模块：

- `profileService`
- `signalService`
- `knowledgeService`
- `recommendationEngine`
- `reportBuilder`
- `hardwareAdapters`

### 11.3 数据层

先用：

- `src/data/mockProfiles.ts`
- `src/data/mockSleepEvents.ts`
- `src/data/knowledgeBase.ts`
- `src/data/mockDailySignals.ts`

后续替换：

- soundcore SDK。
- eufy SDK。
- API 数据。
- 本地存储。

### 11.4 AI 层

第一阶段：

- 规则引擎。
- 模板生成。
- 知识库匹配。

第二阶段：

- LLM structured output。
- Vision model。
- Audio classifier。

第三阶段：

- 端侧模型。
- TFLite / MediaPipe。
- 设备端隐私处理。

## 12. 本任务结论

技术上完全可以先跑通。

最关键的工程策略是：

> 所有真实硬件和 AI 模型都通过 adapter 接入；当前先用 mock 数据和规则引擎跑通闭环，现场再替换真实 SDK、音频文件、图片文件或 API Key。

这条路线能保证：

- 没有数据源时能演示。
- 没有 API Key 时能演示。
- 没有 SDK 文档时能演示。
- 现场拿到硬件能力后能快速增强。
